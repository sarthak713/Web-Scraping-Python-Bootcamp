# Tuples
    # ordered & immutable containers of elements
    # denoted using parentheses, though optional
    # each element has a zero-based index
    # typically used to store values that are closely related together

user1=('Ronaldo',30)
print(type(user1))

user2='Donald',40
print(type(user2))

print(user1[0])

# user1[0]="Sarthak"    # Error

scores = 790,780,640
print(scores)